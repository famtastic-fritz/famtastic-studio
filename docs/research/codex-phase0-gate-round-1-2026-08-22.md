# Codex Phase 0 exit gate, round 1

Verdict: NOT SAFE (6 blockers, 8 major).

[SEVERITY: BLOCKER] [SURFACE: 1] [CLAIM: P0-I1 is currently clean by text search, but it does not constrain the server’s real import graph.] [EVIDENCE: server/kernel/modules.js:10-14 imports computed module URLs; tests/invariant-p0-i1.test.js:13-25,43-48 follows only literal imports; server/kernel/invariants.js:10 defines an unused module deny-list; absence proof: no current runtime forbidden reference exists outside invariants.js.] [FAILURE MODE: An auto-discovered module can import/register the legacy proof pipeline or proof-output paths while the invariant test remains green.] [MINIMAL FIX: Preflight the exact discovered module closure before importing it, resolve realpaths, reject external/symlink escapes and forbidden config roots, then assert the registered route table contains no proof ingress.]

[SEVERITY: BLOCKER] [SURFACE: 2] [CLAIM: The A1 identity pair is neither mandatory nor immutable.] [EVIDENCE: server/kernel/app.js:21-30 establishes no ingress identity; server/kernel/identity.js:3-4 silently prefers query identity over conflicting headers and :11 permits null conversation_id; server/modules/sites/index.js:46-78 reads every site spec without site_id; server/kernel/events.js:51-56 accepts unbound WebSockets; server/kernel/journal.js:8-18 and server/kernel/events.js:27-41 accept caller-supplied site_id and omit conversation_id.] [FAILURE MODE: A request bound to Site A can supply or propagate Site B to a writer/emitter; conflicting identities never produce the required 409.] [MINIMAL FIX: Bind and validate the pair once at HTTP/WS ingress, reject conflicts, pass an immutable identity context into handlers, journal, and events, and explicitly mark truly global routes.]

[SEVERITY: MAJOR] [SURFACE: 2] [CLAIM: no-ambient-site is a name-pattern grep, not enforcement of A1.] [EVIDENCE: scripts/lint-no-ambient-site.mjs:27-35 recognizes six spellings and :49-63 only two named functions; it does not verify route or WebSocket identity binding. The current unbound site inventory and WebSocket paths pass npm run lint.] [FAILURE MODE: const currentSite, let selectedSite, bracketed globals, env aliases, helper renames, or an unbound route all evade the gate.] [MINIMAL FIX: Use AST-based checks plus route-manifest coverage requiring every site-scoped HTTP/WS handler to declare and receive bound identity.]

[SEVERITY: BLOCKER] [SURFACE: 3] [CLAIM: The WebSocket event stream has neither site isolation nor the A5 reconnect contract.] [EVIDENCE: server/kernel/events.js:40 broadcasts every event to every client; :51-57 stores sockets without identity, cursor, message handling, or subscription; :44-49 implements an internal replay helper that is never connected to WebSocket ingress; absence proof: no resync_required, cursor protocol, deduplication, or browser WebSocket client exists.] [FAILURE MODE: Clients receive other sites’ events and lose all events emitted while disconnected.] [MINIMAL FIX: Bind each connection to site identity and last sequence, replay from seq+1, deduplicate by event_id, and return resync_required when retained history cannot satisfy the cursor.]

[SEVERITY: BLOCKER] [SURFACE: 3] [CLAIM: Per-site sequence allocation is unsafe under concurrent instances.] [EVIDENCE: server/kernel/events.js:8,12-24 keeps an instance-local cache and derives the next value without locking; tests/kernel-events.test.js:92-101 tests only a sequential second instance. A read-only two-instance probe produced seq 1 and seq 1 for the same site.] [FAILURE MODE: Overlapping preview/restart processes sharing an events root emit duplicate sequences, making replay order and deduplication ambiguous.] [MINIMAL FIX: Move sequence allocation into a single-writer or locked transactional store and add simultaneous-instance, restart, and corrupt-tail tests.]

[SEVERITY: MAJOR] [SURFACE: 3] [CLAIM: A5’s envelope validator and crash-durable persist-before-broadcast guarantee are absent.] [EVIDENCE: server/kernel/events.js:27-35 validates only truthy type/site_id and creates a non-ULID event_id; :38-40 uses appendFileSync without fsync before broadcast; :47-48 blindly parses every JSONL line; tests/kernel-events.test.js:29-51 exercises only the happy path.] [FAILURE MODE: Invalid envelopes enter the stream, a power loss can erase an already-broadcast event, and a torn final line breaks every replay.] [MINIMAL FIX: Validate and serialize the complete envelope before sequence allocation, generate a real ULID, durably append before send, recover or quarantine torn tails, and inject persistence failures in tests.]

[SEVERITY: BLOCKER] [SURFACE: 4] [CLAIM: The five A6 predicates are implemented with different definitions and without required membership evidence.] [EVIDENCE: server/modules/sites/index.js:6-13 defines valid_spec as JSON parsing, customer without the exclusion list, deployable from spec.deploy.target, and live from live_verified_at; :16-31 and :65-74 repeat those shortcuts and return only id/class/mtime. No schema validator, exclusion-list version, build receipt, canonical deploy receipt, probe freshness, or membership-evidence implementation exists.] [FAILURE MODE: Arbitrary JSON becomes valid, demos become customers, a configured string becomes deployable, and a stale self-asserted timestamp renders as green “live.”] [MINIMAL FIX: Add one shared predicate module implementing the exact five rules from schema/config/receipts/probes and return auditable evidence for every membership.]

[SEVERITY: MAJOR] [SURFACE: 5] [CLAIM: Several endpoints assert emptiness or safety without reading their purported source.] [EVIDENCE: server/modules/operations/index.js:25-44 always returns empty deployments/builds regardless of root contents; :52-55 hardcodes kill_switch.engaged=false; server/modules/platform/index.js:24-34 hardcodes an empty proof feed labeled source connections.] [FAILURE MODE: Existing receipts, DNA runs, proof events, or an engaged automation stop can be hidden behind an honest-looking empty page or false disabled signal.] [MINIMAL FIX: Return explicit not_configured/not_implemented until a real reader exists; once wired, enumerate and validate the source before emitting empty or safe.]

[SEVERITY: MAJOR] [SURFACE: 5] [CLAIM: Missing, malformed, and unreadable site specs collapse into the same empty state.] [EVIDENCE: server/modules/sites/index.js:16-23 returns null for absence, parse failure, and read failure; :80-87 converts null into HTTP 200 status empty; public/kit/region.js:95-100 renders that as ordinary emptiness.] [FAILURE MODE: Corruption or filesystem failure tells the operator that the site merely has no spec.] [MINIMAL FIX: Preserve typed outcomes—missing, invalid_schema, malformed, and io_error—and render failures as errors with evidence.]

[SEVERITY: MAJOR] [SURFACE: 5] [CLAIM: The console-kit accepts malformed 2xx bodies and cannot prevent cross-site stale rendering.] [EVIDENCE: public/kit/region.js:21-25 treats the first array found as the collection; :84-114 accepts any object without endpoint schema validation; :36-56 keys freshness only by a local generation counter; :125-135 reuses lastGoodBody without site/page/revision identity.] [FAILURE MODE: `{}` or the wrong response shape can render as empty/success, and a failed refresh after switching sites can redisplay Site A under Site B.] [MINIMAL FIX: Validate per-endpoint response schemas and bind each request/result cache to immutable site, page, and revision keys.]

[SEVERITY: BLOCKER] [SURFACE: 6] [CLAIM: The Playwright smoke certifies shell presence, not console functionality.] [EVIDENCE: scripts/playwright-smoke.mjs:128-150 directly opens configured URLs without exercising navigation; :153-181 passes on HTML 200, one h1, no pageerror, and no undefined/NaN only; it asserts neither eleven-page inventory nor API/asset responses, region state, rail placement, or interactions.] [FAILURE MODE: Every backing API can return 404/500 and render an error while all eleven pages still pass; a shortened pages.json can also pass.] [MINIMAL FIX: Assert the exact page inventory, click every nav link, fail on request/console errors, verify required assets and APIs, and assert each region reaches the expected truthful state.]

[SEVERITY: MAJOR] [SURFACE: 6] [CLAIM: The A10 local-Playwright requirement is recorded but not gated.] [EVIDENCE: scripts/playwright-smoke.mjs:65-87 can return a null or outside-tree package path; :121-126 only records it; :177-181 excludes package location/version/executable from pass criteria. The present report is local, but the script would also pass with a parent-resolved installation.] [FAILURE MODE: The previously named ghost-dependency defect can recur while smoke remains green.] [MINIMAL FIX: Assert the real package.json path is inside this tree, its version equals the pin, and the selected executable exists before testing pages.]

[SEVERITY: BLOCKER] [SURFACE: 7] [CLAIM: The frozen Phase 1 salvage census cannot satisfy A11’s completeness gate.] [EVIDENCE: docs/salvage/census-2026-08-22.json:17-23 reports 176 files but stores only largest_modules; :531 begins routes for main only; :2939-3022 stores branch deltas as method/path strings; no source SHA appears. ../../FAMtastic/site-studio/scripts/salvage/census.mjs:14 scans only literal-path registrations and :99 discards all but fifteen modules. Consequently middleware such as ../../FAMtastic/site-studio/server.js:1086-1115 is missed.] [FAILURE MODE: “Fully dispositioned” can be declared while most modules, nonliteral middleware, and behavior changes behind unchanged route paths were never reviewed.] [MINIMAL FIX: Regenerate a SHA-bound census containing every non-harness file and every tree’s complete registrations, content deltas, dynamic/unknown registrations, exclusions, and source hashes.]

[SEVERITY: MAJOR] [SURFACE: 7] [CLAIM: The Phase 1 resumability surface names an execution lane that does not exist.] [EVIDENCE: docs/plans/BRIEF-phase-1-2026-08-22.md:53-58 marks Phase 1 active on studio-rebuild/phase-1; docs/decisions/premises/phase-0.md:31-35 still has an unchecked exit and blank tested SHA; absence proof: no local/remote phase-1 branch or worktree exists, and HEAD remains studio-rebuild/phase-0.] [FAILURE MODE: An agent following the active brief writes Phase 1 work directly onto the unclosed Phase 0 lane.] [MINIMAL FIX: Reconcile the brief with live git state and create the Phase 1 branch at the exact tested Phase 0 SHA before implementation starts.]

[SEVERITY: MAJOR] [SURFACE: 7] [CLAIM: Local agent guidance weakens and mispoints the binding amendments.] [EVIDENCE: CONVENTIONS.md:3 points to nonexistent ../site-studio; :13 makes conversation_id conditional; :17 omits replay/resync; .claude/skills/site-studio-conventions/SKILL.md:28-29 instructs emit/journal calls with site_id only. The binding requirements are ../../FAMtastic/site-studio/docs/plans/SITE-STUDIO-REBUILD-PLAN-v1.1-AMENDMENTS-2026-08-21.md:7,19.] [FAILURE MODE: Phase 1 agents can follow the installed project skill exactly and continue violating A1/A5.] [MINIMAL FIX: Correct the authority path, carry the complete binding/replay rules into the short form, and add a conformance check against v1.1.2.]

VERDICT: NOT SAFE. FIX FIRST: make P0-I1 preflight and constrain the exact auto-loaded module closure before any Phase 1 code is added.