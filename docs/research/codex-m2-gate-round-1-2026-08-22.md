# Codex M2 exit gate, round 1

Reviewed at 85933f0c, which predates the PROVE, shadow and deploy-plan work. Verdict: NOT SAFE. Several findings were already fixed at review time; the orchestrator's dispositions follow in docs/decisions/ADR-0005.

TARGET REVIEWED: `85933f0c`. The branch advanced to `4210ddc6` during review; later commits were not credited. Recorded POST-SHIP cuts were excluded.

1. [SEVERITY: BLOCKER] [SURFACE: 1] [CLAIM: The exported shadow runner can write beneath live proof roots before validating its boundary.] [EVIDENCE: `server/kernel/shadow.js:93-116,270-285` creates `SHADOW_DATA_ROOT` and its run directory before checking only against the greenfield data root; absence proof: no comparison with proof-jobs, proof-output, campaigns, blobs, or legacy ledgers, and no `realpath`/`lstat` validation.] [FAILURE MODE: Setting `SHADOW_DATA_ROOT` to a production directory—or to a symlink into one—creates shadow files inside the live proof tree despite the zero-write guarantee.] [MINIMAL FIX: Canonicalize and allowlist the shadow base before the first `mkdir`; reject symlinks and ancestors/descendants of every production filesystem root.]

2. [SEVERITY: BLOCKER] [SURFACE: 1] [CLAIM: G4-1 is fail-open and tests a synthetic fixture instead of the shipped shadow runner.] [EVIDENCE: `scripts/gate-g4-1-shadow-boundary.mjs:38-46,72-80,212-221,307-370` hardcodes one config root, permanently skips proof roots, writes one fixture file, and exits zero whenever `fail === 0`; `docs/research/revenue-safety-gates-2026-08-22.md:80,104-105,127,143` reports PASS while acknowledging four unobserved surfaces and incorrectly says no shadow implementation exists. Absence proof: no `runShadow` invocation outside its kernel at `85933f0c`.] [FAILURE MODE: G4-1 remains green in the presence of finding 1 and when the actual configured production roots are completely unobserved.] [MINIMAL FIX: Execute the real runner against discovered, canonical roots; treat any required unobservable surface as INCOMPLETE/nonzero; correct the stale outreach and shadow claims.]

3. [SEVERITY: BLOCKER] [SURFACE: 1] [CLAIM: G4-0’s PASS does not prove the producer-to-greenfield contract required by A12.] [EVIDENCE: `scripts/gate-g4-0-contract.mjs:64-94,128-153,177-212` manually recreates the payload, prints current repository heads without enforcing expected SHAs, tests legacy validation helpers, and invents the 202 body locally; `docs/research/revenue-safety-gates-2026-08-22.md:69-76` admits it never invokes a consumer, `accept()`, idempotency, or an actual 202 path.] [FAILURE MODE: The Drupal producer or intended ingress can drift while the duplicated fixture continues to pass.] [MINIMAL FIX: Derive the raw signed fixture from a pinned producer artifact and execute an isolated zero-egress ingress handler through acceptance, 202 response, and idempotency—or report G4-0 incomplete.]

4. [SEVERITY: BLOCKER] [SURFACE: 1] [CLAIM: Shadow isolation relies on process-global environment and fetch mutation, and its comparison record is written to the wrong root.] [EVIDENCE: `server/kernel/shadow.js:126-140,160-177,283-323` replaces `globalThis.fetch`, changes the data-root environment variable, then resolves `shadowStoreDir()` and writes the record before restoring that variable.] [FAILURE MODE: Concurrent runs can restore real fetch or another run’s data root mid-execution; completed records land inside the temporary run root and later list/read calls can honestly-looking report nothing.] [MINIMAL FIX: Inject paths and network clients per invocation, capture the live record store before isolation, and add overlapping-run and post-run retrieval tests.]

5. [SEVERITY: BLOCKER] [SURFACE: 3] [CLAIM: Pipeline retry accepts a site-bound request but executes against the site stored in an arbitrary run record.] [EVIDENCE: `server/modules/pipeline/index.js:59-72` drops `identity.site_id`; `server/kernel/pipeline.js:286-306` assigns `site_id = record.site_id`; `/api/builds` exposes run identifiers across sites in `server/modules/builds/index.js:60-74`.] [FAILURE MODE: A request bound to site A can submit site B’s run ID and retry a research, spec, build, or verify stage against B.] [MINIMAL FIX: Pass the bound site ID into `retryStage` and reject a mismatch before reconstructing inputs or executing anything; add A/B route tests.]

6. [SEVERITY: BLOCKER] [SURFACE: 3] [CLAIM: Cross-site filesystem containment is lexical and follows symlinks and unvalidated receipt paths.] [EVIDENCE: `server/kernel/paths.js:38-54`, `mutation.js:27-32,85-93`, `canvas.js:214-250`, and `deploy.js:140-156,270-285` use resolved string containment followed by ordinary reads/writes; absence proof: no symlink, `lstat`, or `realpath` adversarial test exists.] [FAILURE MODE: A symlink inside site A can alias site B or an external path for canvas, mutation, or deploy; a corrupted receipt containing `../` can write outside its release before verification fails.] [MINIMAL FIX: Reject symlinks, canonicalize every path component, validate every persisted manifest path and digest before mutation, and stage restores before atomic replacement.]

7. [SEVERITY: BLOCKER] [SURFACE: 4] [CLAIM: The deploy action dispatches on the first POST with no plan/confirmation boundary.] [EVIDENCE: `server/modules/deploy/index.js:34-42` calls `deploy.deploy()` immediately; absence proof: no deploy-plan, confirmation token, or manifest-hash confirmation route exists. The ratified PROVE contract requires “Deploy confirmation without dispatch” at `SITE-STUDIO-REBUILD-PLAN-2026-08-21.md:226`.] [FAILURE MODE: A mistaken or stale operator action creates receipts, journal entries, copied artifacts, and spec mutations without previewing the exact release.] [MINIMAL FIX: Add a read-only deploy plan returning the manifest hash, then require an explicit confirmation token bound to site, revision, and that hash.]

8. [SEVERITY: BLOCKER] [SURFACE: 4] [CLAIM: Go-live and rollback have no oracle for the active customer-facing target.] [EVIDENCE: `server/kernel/deploy.js:313-380` explicitly performs no DNS check yet manufactures `environment: 'production'`, `verified_at`, and journal status `live`; `deploy.js:265-310` merely rewrites and verifies the already-immutable prior release directory without switching or updating any active pointer, spec target, or DNS state.] [FAILURE MODE: A local directory can be declared live without resolving a domain, and rollback can report success while traffic still targets the bad release.] [MINIMAL FIX: Model an explicit active target; require DNS/HTTP evidence tied to the receipt for go-live, and atomically switch and verify that active target during rollback.]

9. [SEVERITY: BLOCKER] [SURFACE: 4] [CLAIM: A deploy receipt neither limits the publishable artifact nor proves copying completed.] [EVIDENCE: `server/kernel/deploy.js:133-137,173-219` manifests the entire site directory—including operational files such as `spec.json`—then journals `status: deployed` and persists the immutable receipt before copying bytes to the target.] [FAILURE MODE: Internal files can enter the release, and a copy failure leaves durable “deployed” evidence for a missing or partial artifact.] [MINIMAL FIX: Deploy only an explicit verified build manifest; copy to a temporary release, recompute its hash, atomically publish it, and only then finalize the receipt and journal outcome.]

10. [SEVERITY: BLOCKER] [SURFACE: 5] [CLAIM: Successful pipeline DNA is not an A4 replay manifest.] [EVIDENCE: `server/kernel/pipeline.js:57-74` hashes the commit plus porcelain status rather than working-tree contents; `pipeline.js:164-186,203-216,238-248` records digestless refs, null packet/recipe snapshots, and no prompt or usage/cost values; `server/kernel/dna.js:138-159` accepts those null fields.] [FAILURE MODE: Different dirty source bytes can share one tree hash, and a successful record cannot identify the exact packet, recipe, generated outputs, deterministic configuration, or actual zero-cost execution.] [MINIMAL FIX: Persist content-derived tree identity, packet and recipe snapshots/hashes, input/output digests, deterministic composer configuration, and explicit zero usage/cost before allowing a success outcome.]

11. [SEVERITY: BLOCKER] [SURFACE: 5] [CLAIM: Recipe references are decorative, while real successful pipeline runs cannot be saved as recipes.] [EVIDENCE: `server/kernel/pipeline.js:238-248` stores any supplied `recipe_ref` but always runs the default graph with `recipe_snapshot: null`; `pipeline.js:271-277` stores an outcome object, while `server/modules/builds/index.js:114-129` accepts only literal `record.outcome === 'success'`. Absence proof: no recipe execution route exists.] [FAILURE MODE: DNA can claim an arbitrary recipe drove a run that never resolved it, and the actual pipeline-to-save-to-rerun path rejects its own successful records.] [MINIMAL FIX: Normalize the outcome contract and require resolution of an exact versioned recipe snapshot before execution; reject unknown references and add a real pipeline → save → new-input rerun test.]

12. [SEVERITY: BLOCKER] [SURFACE: 5] [CLAIM: The rerun verifier can certify a stage whose verification explicitly failed.] [EVIDENCE: `server/kernel/recipe.js:183-207`, especially line 204, treats `verification.passed === true OR status === 'success'` as passing and never validates the recipe reference, declared new input, or overall outcome.] [FAILURE MODE: `{status:'success', verification:{passed:false}}` is certified as a verified rerun, allowing contradictory or fabricated records to earn success.] [MINIMAL FIX: Explicit false must always fail; require the exact recipe/version, declared new input, successful outcome, and each stage’s stated verification requirement.]

13. [SEVERITY: MAJOR] [SURFACE: 2] [CLAIM: The deterministic composer produces a broken default primary CTA while verification reports success.] [EVIDENCE: `server/kernel/pipeline.js:81-90` gives home a `cta` section and contact its own page; `server/kernel/compose.js:57-60` hardcodes CTA links to `#contact`; `server/kernel/verify.js:19-25` skips all fragment links.] [FAILURE MODE: The default homepage links to a nonexistent same-page anchor, yet the pipeline records a successful verification.] [MINIMAL FIX: Link to `contact.html` or emit a matching home anchor, and verify that same-page fragment targets exist.]

14. [SEVERITY: MAJOR] [SURFACE: 2] [CLAIM: Proofs converts unreadable evidence and not-configured state into an authoritative-looking empty state.] [EVIDENCE: `server/kernel/proofs.js:86-115` suppresses every replay error and can return “no proof events”; `proofs.js:51-76` emits `not_configured`, but `public/kit/region.js:115-142` has no distinct renderer for it; `scripts/region-honesty.mjs:12-28` reproduces the same collapse.] [FAILURE MODE: Corrupt logs, permission failures, and absent configuration look indistinguishable from a verified absence of proofs.] [MINIMAL FIX: Return error/partial status with failure counts and render `not_configured` distinctly from `empty`; add malformed and unreadable log cases.]

15. [SEVERITY: MAJOR] [SURFACE: 6] [CLAIM: The requested exit SHA is neither revenue-gate-complete nor clean under its declared gate command.] [EVIDENCE: `package.json:8-14` omits both G4 scripts; `scripts/lint-no-ambient-site.mjs:29-32` necessarily rejects the `globalThis.fetch =` assignments at `server/kernel/shadow.js:130,139`; absence proof at `85933f0c`: no `tests/prove.test.js`, shadow module/UI, kernel shadow test, or commit-bound PROVE artifact.] [FAILURE MODE: The nominal gate does not execute the revenue claims and cannot pass the exact candidate anyway, leaving critical action and populated-state paths outside exit proof.] [MINIMAL FIX: Fix the lint violation, wire G4-0/G4-1 and committed PROVE/action smokes into one final-SHA gate, and require that exact command to pass before packet assembly.]

VERDICT: NOT SAFE

FIX FIRST: Close the shadow-root boundary before its first filesystem write, then make G4-1 exercise that exact runner and fail nonzero for every unobserved production surface.