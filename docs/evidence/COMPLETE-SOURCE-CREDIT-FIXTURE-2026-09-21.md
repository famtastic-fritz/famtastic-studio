# Completed-source credit fixture repair

Source-only, synthetic and offline. Studio base: `8f476ca`; paired Designs
fixture: `scripts/test-selected-source-intent.php` on `codex/selected-credit-policy`.
No production Node/PHP source or callback limits changed.

The normal artifact pipeline builds one independent synthetic repository from
already-branded Home and the pinned 2,020,725-byte PNG. Generation providers are
forbidden by the existing fixture. No selected job, association or durable
mapping exists initially. Real static browser QA and finalized-source export
precede the first agency adoption. The PHP fixture writes supplied original bytes
inside its own temporary directory, derives actual hashes/lengths, and uses the
existing confined `selected_build_artifacts` seam. Both `authority.files` entries
carry explicit rights; PNG rights are creator-attribution-only, publication-false.

The old raw Home, missing PNG bytes/authority/rights, changed asset bytes,
consent-only rights, conflicting duplicate, cross-account identity and bad export
or scope digests remain rejected. Exact retries reuse the same job and repository;
the returned source receipt passes the real PHP receipt service without checkout
eligibility. The existing object/list, numeric spelling, safe-integer, Unicode,
escaping and raw JSON byte-identity cases remain. Correct per-URL binary delivery
is asserted. The fixture separately rejects duplicate files and traversal input.

Executed with Node 24.19.0, PHP 8.5.9 and the existing dependency symlink after
exact package.json/package-lock.json comparison: one Vitest test/file passed in
14 seconds. The existing PHP portal-selection harness passes all three cases;
syntax and whitespace checks pass. External network, current runtime data,
credential directories and canonical checkout writes were sandbox-denied.
All hosting/callback transports were injected local fixtures. No install,
full suite, provider call, customer write, deployment or push occurred.

Focused command, from Studio under the same offline guard, with at least 200 MiB
free before starting:

```sh
SELECTED_SOURCE_INTENT_HARNESS=/tmp/famtastic-credit-policy.zKVqJI/designs/scripts/test-selected-source-intent.php \
SELECTED_STAGING_AGENCY_HARNESS=/tmp/famtastic-credit-policy.zKVqJI/designs/scripts/test-selected-staging-contract.php \
node node_modules/vitest/vitest.mjs run tests/source-export-roundtrip.test.js \
  --no-file-parallelism --maxWorkers=1 --testTimeout=120000 --reporter=dot
```

The subprocess output buffer is bounded at 16 MiB because the existing wire
retains the inline original PNG. This is a test-only buffer, not a production
wire-budget increase. Callback PNG support, original-archive/metadata-only wire
hardening, and the parent's lineage/customer-binding fixes remain separate.
Full merged-suite verification belongs to the parent after integration.
